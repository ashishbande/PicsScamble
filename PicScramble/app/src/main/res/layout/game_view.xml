<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rl"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#c2dbfd">
    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="vertical"
        >
    </android.support.v7.widget.RecyclerView>


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/timeView"
        android:text="@string/title_text_gameView"
        android:layout_below="@id/recycler_view"
        android:layout_centerHorizontal="true" />

    <android.support.v7.widget.CardView
        android:id="@+id/random_card_view"
        android:layout_width="@dimen/card_width"
        android:layout_height="@dimen/card_height"
        android:layout_below="@id/timeView"
        card_view:cardCornerRadius="@dimen/card_padding"
        card_view:cardElevation="@dimen/card_padding"
        card_view:cardMaxElevation="@dimen/card_padding"
        card_view:contentPadding="@dimen/card_padding">

    <ImageView
        android:id="@+id/random_image_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:gravity="center"
        android:padding="10dp"
        android:scaleType="fitXY">

    </ImageView>
    </android.support.v7.widget.CardView>>

</RelativeLayout>